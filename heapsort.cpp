#include <iostream>
using namespace std;

void heap_sort(int *array, int n) {

}

void down_to_top_adjust(int *array, int index) {
    int neighbour, parent;
    if(index % 2 == 0)
        neighbour = index - 1;
    else
        neighbour = index + 1;
    parent = (index - 1)/2;
    
}

int main(){

}